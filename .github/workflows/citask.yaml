name: GitHub Action
on: [ push ]
jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
     - uses: actions/checkout@v2
     - run: echo "This job was automatically triggered by ${{ github.event_name }} event."
     - run: JavaScriptCodingChallenge