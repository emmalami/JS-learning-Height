// Calculate Area of Rectangle and console log the product

function calculateAreaOfRectangle (lenght , width){
    const product = lenght*width;
    console.log(product);
}
calculateAreaOfRectangle(12,5)