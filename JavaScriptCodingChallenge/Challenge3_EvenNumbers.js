//prints out all the Even numbers between 0 to 10

for(let number = 0; number <= 10; number = number +1){ //initiializing the start point, state the condition and increament
    if(number % 2 === 0){
        console.log(number +' is an Even number')
    }
}